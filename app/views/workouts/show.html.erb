<p id="notice"><%= notice %></p>

<p>
  <b>Workout date:</b>
  <%= @workout.workout_date %>
  <% if @workout.exercises.any? %>
  <ul>
  <% @workout.exercises.each do |exercise| %>
  <li><%= exercise.lift.lift_name %></li>
  <% end %>
  </ul>
  <% end %>
</p>


<%= link_to 'Edit', edit_workout_path(@workout) %> |
<%= link_to 'Back', workouts_path %>
